# Asset Slots

Use these folders for licensed 3D content:

- `planets`: worlds, moons, asteroid fields
- `fleets`: ships, drones, carriers
- `stations`: starbases, orbital structures, gates

The starter prototype currently uses generated primitive geometry in code.
